apiVersion: v1
kind: Service
metadata:
  name: {{ include "mr_m4_helm_charts.fullname" . }}
  labels:
    {{- include "mr_m4_helm_charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mr_m4_helm_charts.selectorLabels" . | nindent 4 }}
