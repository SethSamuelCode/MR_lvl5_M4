apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mr_m4_helm_charts.fullname" . }}-test-connection"
  labels:
    {{- include "mr_m4_helm_charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mr_m4_helm_charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
